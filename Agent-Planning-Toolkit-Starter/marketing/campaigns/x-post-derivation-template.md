# X Post Derivation Template (280 Characters)

Derive X posts from the YouTube short core message.

Use channel defaults from `../channel-defaults.md`.

## Inputs

- Core hook:
- Outcome promise:
- CTA keyword:
- Optional proof cue:

## Output contract

Generate exactly three variants:

1. `Signal` post (direct positioning)
2. `Proof` post (claim + proof cue)
3. `CTA` post (invitation + keyword)

Rules for each:

- Maximum 280 characters.
- One idea per post.
- Same promise as YouTube short.
- Optional hashtags: 0-2.
- No method/IP disclosure.

## Template patterns

### Signal

`<Hook>. <Outcome promise>. Reply <CTA keyword>.`

### Proof

`<Claim>. Proof: <proof cue>. Reply <CTA keyword>.`

### CTA

`<Pain reframe>. <Outcome promise>. Message <CTA keyword>.`
